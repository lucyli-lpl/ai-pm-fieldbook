---
title: 多模态文档识别可用但不稳定
---

**结论**：多模态能力在文档识别场景里已经展现出价值（比如识别上传文档的类型），但一致性还不够，不能完全依赖。

## 财务报销助手

报销场景中多模态可以识别用户上传单据的类型，但表现不够稳定，同一类单据的识别结果存在波动。

## 判断

按能力三区的语言：多模态文档识别目前是**边缘区**能力——可以用，但产品承诺只能是"尽力识别、识别不了时正确处理"，不能是"稳定识别"。验收时要同时看识别成功率和识别失败时的降级是否正确。

> 对应模块 02 复杂场景判区。
